import EventEmitter from 'events';
import { keyBy } from 'lodash';

export const types = {
	shutdown: 'shutdown'
};
export default () => {
	const emitter = new EventEmitter();
	emitter.types = types;
	return emitter;
};
