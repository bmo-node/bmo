import thing from './thing';
const repositories = {
	thing
};
export default (config) => {
	const instances = config.dependencies.repositories = {};
	Object.keys(repositories).forEach(async key => {
		instances[key] = repositories[key](config);
	});
	return instances;
};
