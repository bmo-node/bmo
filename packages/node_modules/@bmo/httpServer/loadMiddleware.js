import bodyparser from 'koa-bodyparser';
import logger from 'koa-pino-logger';
import helmet from 'koa-helmet';
export default async (config, app) => {
	app.use(helmet());
	app.use(bodyparser());
	app.use(logger());
	if (config.loadMiddleware) {
		config.loadMiddleware(config, app);
	}
};
