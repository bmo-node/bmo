import loadRoutes from '@bmo/loadRoutes'
import thing from './thing'
const routes = [thing]
const prefix = '/api'
export default (config, Router) => loadRoutes(routes.map(r => r(config, Router)), new Router({ prefix }))
