import Vue from 'vue'
import Vuex from 'vuex'
import axios from 'axios'
Vue.use(Vuex)
const actions = {
  increment: 'increment',
  decrement: 'decrement'
}
export default new Vuex.Store({
  state: {
    count: 1
  },
  mutations: {
    increment: (state, amount) => {
      state.count += amount
    },
    decrement: (state, amount) => {
      state.count -= amount
    },
    setThing: (state, thing) => {
      state.thing = thing
    }
  },
  actions: {
    increment: async (context, amount) => {
      // context.commit(actions.increment, amount)
      const thing = await axios.get(somthing)
      context.commit(action.setThing, thing)
    },
    decrement: (context, amount) => {
      context.commit(actions.decrement, amount)
    }
  }
})
