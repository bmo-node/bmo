import app from './app'
import config from './config'
import dependencies from './dependencies'

export default async (runConfig) => {
	const appConfig = await config(runConfig)
	const services = await dependencies(appConfig)
	appConfig.services = services
	const appInstance = await app(appConfig)
	return appInstance
}
