import Router from 'koa-router'
import loadRoutes from '@bmo/loadRoutes'
import api from './api'
import health from './health'
const routes = [
	api,
	health
]
export default (config) => loadRoutes(routes.map(r => r(config, Router)), new Router())
