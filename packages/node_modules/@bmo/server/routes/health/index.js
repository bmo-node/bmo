import { OK } from 'http-status-codes';

export const handler = (ctx, next) => {
	ctx.body = { health: 'OK' };
	ctx.status = OK;
};

export default () => [{ method: 'GET', path: '/health', handler }];
