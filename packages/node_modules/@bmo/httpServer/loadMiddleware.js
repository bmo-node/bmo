import bodyparser from 'koa-bodyparser';
import logger from 'koa-pino-logger';
import helmet from 'koa-helmet';
import errorHandler from './errorHandler';
export default async (manifest, app) => {
	app.use(errorHandler(manifest));
	app.use(helmet());
	app.use(bodyparser());
	app.use(logger());
};
