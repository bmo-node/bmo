import loadRoutes from '@bmo/loadRoutes'
import v1 from './v1'

const versions = [v1]
const prefix = '/things'

export default (config, Router) => loadRoutes(
	versions.map(v => v(config, Router)),
	new Router({ prefix })
)
