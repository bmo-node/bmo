
export default (config, app, router, view, proxy, staticMiddleware) => {
	const baseUrl = view.baseUrl || '/';
	if (config.dev) {
		console.log('Proxying ' + view.devHost);
		app.use(proxy({ host: view.devHost }));
	} else {
		app.use(staticMiddleware(view.staticAssets));
	}
	return router;
};
